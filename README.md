# Ganesh V — Portfolio (Static + Animated)

Open locally (recommended):  
- `python -m http.server 8000` then open `http://localhost:8000`

Deploy:
- GitHub Pages / Netlify / Vercel

Entry point: `index.html`
